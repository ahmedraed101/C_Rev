#include <stdio.h>

int main(void)
{
    printf("the keyword is reserved words in C language that have a special meaning\nand can't be used as identifiers\n");
    printf("int age;\nthis statement (int) is the keyword and (age) is the identifier\n");
    return(0);
}